{
  "version": 2,
  "builds": [
    {
      "src": "project/manage.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    { "src": "/static/(.*)", "dest": "/project/static/$1" },
    { "src": "/(.*)", "dest": "/project/manage.py" }
  ]
}
